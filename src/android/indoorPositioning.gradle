repositories{
  jcenter()
  flatDir {
      dirs 'libs'
   }
}

dependencies {
   compile(name:'IndoorPositioningSdk', ext:'aar')
}

android {
  defaultConfig {
      minSdkVersion 21
      targetSdkVersion 21
  }

  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}
